<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Jae Yeong's Longin</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="/script/login.js"></script>
    <style>
        body {
            background-color: skyblue;
        }
        #login {
            display: table;
            margin : 100px auto;
            font-size : 20px;
        }

        input {
            font-size : 20px;
            margin-bottom : 10px;
            margin-top : 10px;
        }

        input[type="submit"]{
            font-size : 20px;
            padding : 10 30px;
        }

        input[type="submit"]:hover{
            background-color: rgb(160, 160, 160);
        }


    </style>
  
</head>
<body>
    <div id="login">
        <h1>로그인 페이지</h1>
        <form action="/" method="post">
            <!-- not found error : 타입자체가 undefined -->
            <input type="text" id="result" value="<%= (typeof(result) !== 'undefined' ? result : '') %>" hidden>
            <input type="text" placeholder="Id" name="user_id" required><br>
            <input type="password" placeholder="Password" name="user_pw" required><br>
            <br>
            <input type="submit" value="로그인" id="btn_login">
        </form>
    </div>
    
</body>
</html>